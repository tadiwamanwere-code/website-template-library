# Healcure — Healthcare Landing Hero

- **Source:** exact user spec, Tailwind CDN build, bottom-aligned hero (`justify-end`), no logo banners/proudly-worked-with section per exclusion.
- **Steal this technique:** word-by-word reveal via JS `splitToWords()` — wraps every word in its own `span` with a staggered `animation-delay`, so the heading/subtext types itself in with blur+translateY rather than a single fade. Cheap to implement, reads as expensive.
- **Accent color:** none — pure white/black on photo, one white "Trusted" pill as the only chip.
