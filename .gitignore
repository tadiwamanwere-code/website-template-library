node_modules/
.playwright-mcp/
.vercel/
*.log
.DS_Store
Thumbs.db

# agent verification screenshots — debris, not reference material
_inspo-notes/verification/

.vercel

# builder scratch output — rendered test sites, not source
builder/out/
builder/sites/

# keys — never commit
builder/.env
.env
.env.local

# skills installed by npx skills
.agents/

# installed by npx skills — tooling, not part of the library
.claude/
skills-lock.json
