# Velora — Dark Luxury Car Rental Hero

- **Source:** exact user spec, built verbatim (single self-contained hero component, no extra sections).
- **Steal this technique:** the giant 13.3vw "REGAL DRIVE" headline sits *behind* the nav/copy (z-index 2 vs 3) with a white→gray vertical gradient text-clip — pure typography as hero art, no photo needed. Combined with the bulletproof video-autoplay watchdog (play() retried on pause/ended/visibilitychange + 1s interval) so the background video truly never stops.
- **Accent color:** none — pure black/white/glass. Zero color accent by design (luxury restraint).
